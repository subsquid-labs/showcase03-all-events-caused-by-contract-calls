manifestVersion: subsquid.io/v0.1
name: showcase03-all-events-caused-by-contract-calls
version: 1
description: |-
  The very first evm squid from manifest
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "sqd", "process:prod" ]
  api:
    cmd: [ "sqd", "serve:prod" ]
